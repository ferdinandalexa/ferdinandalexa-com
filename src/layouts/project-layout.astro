---
import ProjectHeader from "../components/project-header/project-header.astro";
import BaseLayout from "./base-layout.astro";

const {
  url,
  content: { title, type, description, tools, demo },
} = Astro.props;
---

<style>
  div,
  main {
    display: flex;
    flex-direction: column;
    gap: 4.5rem;
  }

  main {
    margin-top: 6rem;
  }
</style>

<style is:global>
  h2 {
    margin-bottom: 2rem;
    color: var(--blue-300);
    font-size: var(--fs-3xlarge);
    font-weight: var(--fw-bold);
  }

  h3 {
    color: var(--blue-300);
  }

  html[scheme-mode="dark"] :is(h2, h3) {
    color: var(--blue-100);
  }
</style>

<BaseLayout
  title={`${title} | ferdinandalexa`}
  description={description}
  slug={url}
>
  <main>
    <ProjectHeader
      type={type}
      title={title}
      description={description}
      tools={tools}
      url={url}
      demo={demo}
    />
    <section aria-labelledby="process">
      {type.includes("UX/UI") && <h2 id="process">Proceso</h2>}
      <div>
        <slot />
      </div>
    </section>
  </main>
</BaseLayout>
