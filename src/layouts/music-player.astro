<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex" />
    <title>05.09.24</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  @font-face {
    font-family: "Sticky Pops";
    font-weight: 400;
    font-style: normal;
    font-display: swap;
    src:
      local("Sticky Pops"),
      local("StickyPops"),
      url("/fonts/Sticky Pops.otf") format("opentype");
  }

  @font-face {
    font-family: "Wasted Year";
    font-weight: 400;
    font-style: normal;
    font-display: swap;
    src:
      local("Wasted Year"),
      local("WastedYearRegular"),
      url("/fonts/WastedYear.otf") format("opentype");
  }

  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  body {
    position: relative;

    margin: 0;
    padding: 0;

    color: #fff;

    background-color: #a6f538;
  }

  body::before {
    content: "";

    position: absolute;
    z-index: -1;

    width: 100%;
    height: 100%;

    opacity: 0.1;
    background-image: url("/images/vinilo/texture.webp");
    background-repeat: repeat;
    background-size: 100vw;
  }

  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    margin: 0;
    padding: 0;
    font-weight: 400;
  }
</style>
