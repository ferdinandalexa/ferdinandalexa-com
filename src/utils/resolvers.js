const rootResolver = {
  en: '/',
  es: '/en',
}

const languageResolver = {
  en: "/es",
  es: "/en",
};

const collectionResolver = {
  proyectos: "/projects",
  projects: "/proyectos",
  blog: "/blog",
};

export { rootResolver, languageResolver, collectionResolver }